apiVersion: v1
kind: Service

metadata:
  name: financial-assistant-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing

spec:
  type: LoadBalancer

  selector:
    app: financial-assistant

  ports:
  - protocol: TCP
    port: 80
    targetPort: 8501